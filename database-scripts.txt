/*[HABER_SITESI] Adında bir veritabanı oluşturduktan sonra bu scripti çalıştırınız**/

USE [HABER_SITESI]
GO
/****** Object:  Table [dbo].[HABERLER]    Script Date: 1.03.2024 15:03:51 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE TABLE [dbo].[HABERLER](
	[ID] [int] IDENTITY(1,1) NOT NULL,
	[BASLIK] [nvarchar](max) NULL,
	[EKLEME_TARIHI] [datetime] NULL,
	[YAZAR_ID] [int] NOT NULL,
	[KATEGORI_ID] [int] NOT NULL,
	[ICERIK] [nvarchar](max) NULL,
	[RESIM] [nvarchar](max) NULL,
	[VIDEO] [nvarchar](max) NULL,
	[GOSTERIM_SAYISI] [int] NULL,
	[AKTIFMI] [bit] NULL,
PRIMARY KEY CLUSTERED 
(
	[ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY] TEXTIMAGE_ON [PRIMARY]
GO
/****** Object:  Table [dbo].[KATEGORILER]    Script Date: 1.03.2024 15:03:51 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE TABLE [dbo].[KATEGORILER](
	[ID] [int] IDENTITY(1,1) NOT NULL,
	[ACIKLAMA] [nvarchar](max) NULL,
	[AKTIFMI] [bit] NULL,
PRIMARY KEY CLUSTERED 
(
	[ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY] TEXTIMAGE_ON [PRIMARY]
GO
/****** Object:  Table [dbo].[SLAYTLAR]    Script Date: 1.03.2024 15:03:51 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE TABLE [dbo].[SLAYTLAR](
	[ID] [int] IDENTITY(1,1) NOT NULL,
	[BASLIK] [nvarchar](max) NULL,
	[ICERIK] [nvarchar](max) NULL,
	[HABER_ID] [int] NOT NULL,
	[RESIM] [nvarchar](max) NULL,
	[AKTIFMI] [bit] NULL,
PRIMARY KEY CLUSTERED 
(
	[ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY] TEXTIMAGE_ON [PRIMARY]
GO
/****** Object:  Table [dbo].[YAZARLAR]    Script Date: 1.03.2024 15:03:51 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE TABLE [dbo].[YAZARLAR](
	[ID] [int] IDENTITY(1,1) NOT NULL,
	[AD] [nvarchar](max) NULL,
	[SOYAD] [nvarchar](max) NULL,
	[EPOSTA] [nvarchar](max) NULL,
	[SIFRE] [nvarchar](max) NULL,
	[RESIM] [nvarchar](max) NULL,
	[AKTIFMI] [bit] NULL,
PRIMARY KEY CLUSTERED 
(
	[ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY] TEXTIMAGE_ON [PRIMARY]
GO
/****** Object:  Table [dbo].[YORUMLAR]    Script Date: 1.03.2024 15:03:51 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE TABLE [dbo].[YORUMLAR](
	[ID] [int] IDENTITY(1,1) NOT NULL,
	[AD] [nvarchar](max) NULL,
	[SOYAD] [nvarchar](max) NULL,
	[EPOSTA] [nvarchar](max) NULL,
	[BASLIK] [nvarchar](max) NULL,
	[ICERIK] [nvarchar](max) NULL,
	[HABER_ID] [int] NOT NULL,
	[EKLEME_TARIHI] [datetime] NULL,
	[AKTIFMI] [bit] NULL,
PRIMARY KEY CLUSTERED 
(
	[ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY] TEXTIMAGE_ON [PRIMARY]
GO
ALTER TABLE [dbo].[HABERLER]  WITH CHECK ADD FOREIGN KEY([KATEGORI_ID])
REFERENCES [dbo].[KATEGORILER] ([ID])
GO
ALTER TABLE [dbo].[HABERLER]  WITH CHECK ADD FOREIGN KEY([KATEGORI_ID])
REFERENCES [dbo].[KATEGORILER] ([ID])
GO
ALTER TABLE [dbo].[HABERLER]  WITH CHECK ADD FOREIGN KEY([YAZAR_ID])
REFERENCES [dbo].[YAZARLAR] ([ID])
GO
ALTER TABLE [dbo].[SLAYTLAR]  WITH CHECK ADD FOREIGN KEY([HABER_ID])
REFERENCES [dbo].[HABERLER] ([ID])
GO
ALTER TABLE [dbo].[YORUMLAR]  WITH CHECK ADD FOREIGN KEY([HABER_ID])
REFERENCES [dbo].[HABERLER] ([ID])
GO
